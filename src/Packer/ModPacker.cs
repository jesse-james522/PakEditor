namespace Packer;

/// <summary>
/// Stages the dirty asset set and invokes tools/retoc/retoc.exe as a subprocess
/// to produce the final .utoc/.ucas/.pak mod trio.
/// </summary>
public class ModPacker
{
}
